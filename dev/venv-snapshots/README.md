# ML virtual environment snapshots

This folder contains `pip freeze` snapshots for the shared `.venv-ml` runtime used during development.

These files are for audit and rollback only. They are not the official installation requirements.

Official ML install inputs remain:

- `requirements-stt.txt`
- `requirements-local-tts.txt`

